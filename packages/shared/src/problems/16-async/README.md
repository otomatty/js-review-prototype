# Ch16. 非同期

`Promise`、 `async` / `await`、 非同期処理の合成。 「結果がすぐには返ってこない処理」 を扱う最後の章。

## 含まれるステージ

- **S4** アルゴリズム — `Promise` の基本、 `async` 関数と `await`、 単一の非同期処理。
- **S5** 設計演習 — `Promise.all` / `Promise.race` などの合成、 エラー伝搬。

## MDN 既定ページ

- [Promise を使う](https://developer.mozilla.org/ja/docs/Web/JavaScript/Guide/Using_promises)

## ディレクトリ構成

```
16-async/
├── README.md
├── _index.ts
├── s4/   # Phase 9 で追加予定
└── s5/   # Phase 9 で追加予定
```

## 状態

問題は未追加。
