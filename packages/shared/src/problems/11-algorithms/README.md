# Ch11. アルゴリズム

探索、 ソート、 累積、 二重ループなどの考え方。 「何を使うか」 ではなく 「どう組み立てるか」 を問う章。 paiza B/A 帯を意識する。

> **🆕 この章は S4 で初めて出ます。** S0-S3 では登場しない「アルゴリズムを自分で組み立てる」 (探索・整列・再帰) を S4 から扱います。

## 含まれるステージ

- **S4** アルゴリズム — 線形探索・二分探索・基本ソート・再帰の入口 (実装済み)。
- **S5** 設計演習 — 計算量を意識した解法選択、 統合課題への組み込み (Phase 9 後半で追加予定)。

## MDN 既定ページ

- [ループと反復処理](https://developer.mozilla.org/ja/docs/Web/JavaScript/Guide/Loops_and_iteration)

## ディレクトリ構成

```
11-algorithms/
├── README.md
├── _index.ts
├── s4/   # 線形/二分探索・ソート・再帰の入口 (実装済み: 6 問 + 卒業課題)
└── s5/   # Phase 9 後半で追加予定
```

## S4 問題ラインナップ

| # | タイトル | 主に学ぶ概念 |
|---|---|---|
| 1 | 線形探索で値の添字を返す | `for` で先頭から走査する素朴な探索 (`O(N)`) |
| 2 | 二分探索でソート済み配列を探す | ソート済み前提で範囲を半分にする (`O(log N)`) |
| 3 | `Array.sort` の比較関数で文字列を長さ順に並べる | `(a, b) => a.length - b.length` の比較ルール |
| 4 | バブルソートを自分で実装する | 二重ループと swap で並べ替える (`O(N^2)`) |
| 5 | 再帰で階乗を求める | ベースケースと再帰呼び出しの組み合わせ (再帰の入口) |
| 6 | 再帰でフィボナッチ数を求める | 2 つに分岐する再帰 — `O(2^N)` の体感 |
| 7 | [卒業課題] 再帰でネスト配列の合計を求める (簡単な DFS) | `Array.isArray` で分岐しつつ構造を潜る木探索の入口 |

## S4 で身につけること

- **計算量への意識付け**: 線形探索 (`O(N)`) と二分探索 (`O(log N)`) を並べて比べ、 「ソート済みかどうか」 が探索コストを変えることを実感する。
- **ソートの中身**: 組込み `.sort` を呼ぶだけでなく、 バブルソートを自分で書いて `O(N^2)` の挙動を理解する (なぜ大きな配列で組込みより遅いのか)。
- **再帰の入口**: ベースケースを書き忘れると無限再帰になることや、 素朴なフィボナッチが `O(2^N)` で爆発することを体感し、 後の章のメモ化・DP へつなげる。
- **DFS の入口**: 卒業課題のネスト配列合計で、 「データ構造そのものを再帰で辿る」 感覚を掴む。
